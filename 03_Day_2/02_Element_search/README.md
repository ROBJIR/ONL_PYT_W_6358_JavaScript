![Coders-Lab-1920px-no-background](https://user-images.githubusercontent.com/30623667/104709394-2cabee80-571f-11eb-9518-ea6a794e558e.png)


## Exercise 1 - done with the lecturer

1. Find the first `h1` element on this page,
2. Find an element with the `title` **class**,
3. Find an element with the `data-animation` attribute

Print out every element you find in the console to make sure your code works.


## Exercise 2 - done with the lecturer

Print out in the console:

 1. how many `div` elements are there on the page? (there are 20 of them),
 2. how many elements with the class `offers` are there on the page? (there are 12 of them),
 3. how many elements on the page have the `href` attribute? (there are 13 of them).


## Exercise 3

Find the following elements on the page and save them in appropriately named variables:
1. Element with the **id** ```home``` (in two ways).
2. The first **li** element that has the ```data-direction``` attribute.
3. The first element with the ```block``` **class**.

Remember to check every time if you have loaded the right element. Remember to use functions that only search for one element.


## Exercise 4

Find the following elements on the page and save them in appropriately named variables:
1. All **li** elements in the ```nav``` **tag** with the class `navigation`.
2. All **paragraphs** belonging to all **div**s.
3. All **div** elements belonging to the ```article``` **tag**.

Remember to check each time to see if you have loaded the right elements. Remember to use functions that search for multiple elements. Each time, print out in the console how many elements you found.


## Exercise 5

1. Find on the page and save in an appropriately named variable the ```article``` **tag** with the  ```first``` **class**.
2. In the next step:
  * print out in the console how many **h2** elements are in this **tag**.

Remember to check each time to see if you have loaded the right elements.
